# Supabase (region: Singapore)
NEXT_PUBLIC_SUPABASE_URL=

# Sistem API key BARU (sb_publishable_… / sb_secret_…), menggantikan anon/service_role lama.
# Publishable key memang ikut ter-bundle di klien — itu wajar, RLS yang menjaganya.
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Hanya untuk skrip server & seeding. Membypass RLS sepenuhnya.
# JANGAN pernah masuk ke bundle klien, dan jangan pernah dikirim lewat chat.
SUPABASE_SECRET_KEY=

# Rahasia penandatangan JWT anak, dipakai Edge Function `child-login`.
# NAMANYA TIDAK BOLEH berprefix SUPABASE_ — prefix itu direservasi Supabase,
# `supabase secrets set SUPABASE_...` ditolak, dan nilainya jadi undefined saat runtime.
#   supabase secrets set CHILD_JWT_SECRET=<jwt secret proyek>
CHILD_JWT_SECRET=

# ── HANYA apps/console ───────────────────────────────────────────────────────
# Gerbang basic-auth console (`apps/console/middleware.ts`). Console membaca service role
# LINTAS KELUARGA, jadi satu URL tanpa gerbang = seluruh saldo semua anak.
# Gagal-tertutup: tanpa nilai ini console menjawab 503 untuk SEMUA permintaan — itu disengaja,
# supaya lupa memasang env terlihat seketika alih-alih membuka pintu diam-diam.
CONSOLE_PASSWORD=

# ── Catatan lokasi (29 Juli 2026) ────────────────────────────────────────────
# Next.js membaca env dari direktori app-nya, bukan dari root repo. Jadi nilai di atas
# perlu ada di `apps/kid/.env.local` (dan nanti `apps/parent/.env.local`), bukan hanya di sini.
#
# NEXT_PUBLIC_*        → ikut ke browser. Wajar: RLS yang menjaga, bukan kerahasiaan kunci.
# SUPABASE_SECRET_KEY  → HANYA server action. Sejak migrasi 0009 ia satu-satunya jalan menulis
#                        ledger, jadi ia juga satu-satunya kunci yang benar-benar berbahaya.
